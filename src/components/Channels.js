import React from 'react';
import {
  ChannelsContainer,
  ChannelsTitle
} from '../Channels/styled'

const Channels = () => {
  return(
    <ChannelsContainer>
      <ChannelsTitle>
        <p>Channels</p>
      </ChannelsTitle>
    </ChannelsContainer>
  )
}

export default Channels;